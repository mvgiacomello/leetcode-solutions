from typing import List


def median_sorted_arrays(nums1: List, nums2: List) -> float:
    """
    https://leetcode.com/problems/median-of-two-sorted-arrays/
    :param nums1: list of integers
    :param nums2: list of integers
    :return: the median of all elements
    """
    if len(nums1) == 0 and len(nums2) == 0:
        raise ValueError('nums argument cannot be both empty')
    raise NotImplementedError('solution not yet implemented')
